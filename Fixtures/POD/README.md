# POD subset fixtures

This directory contains synthetic metadata for testing the OrgRec POD subset
import boundary. It contains no file copied from the MODAVIS Pipe Organ Dataset
and is not a scientific sample of POD Release 1.5.

The production subset is published as a separate dataset. Its version DOI,
source-release identifier, license, inventory, and checksums must be inserted
only after that deposit is final.
